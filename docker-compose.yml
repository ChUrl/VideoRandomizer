version: "3.8"
services:
  db_postgres:
    image: "postgres:14.1-alpine"
    container_name: "db_postgres"
    environment:
      - POSTGRES_DB=videorandomizer
      - POSTGRES_USER=dev
      - POSTGRES_PASSWORD=dev_pass
    volumes:
      - ./postgres/storage:/var/lib/postgresql/data
